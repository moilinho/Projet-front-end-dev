const theme = {
    token: {
      colorPrimary: '#1890ff', // Couleur principale
      colorSecondary: '#52c41a', // Couleur secondaire
      borderRadius: 8, // Rayon des bordures
      fontSizeBase: 14, // Taille de police de base
    },
    components: {
      Button: {
        borderRadius: 8,
      },
    },
  };
  
  export default theme;